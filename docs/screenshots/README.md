# Screenshots

Drop PNGs here to populate the README's Screenshots section. Capture them from the
running desktop app (`./build/desktop/desktop`, with `./build/service/service` running).

| File | What to show |
|---|---|
| `dashboard.png` | Dashboard page: CPU/Memory/GPU bars, FPS, pipeline status, camera table |
| `live-view.png` | Live View: a camera stream with detection bounding boxes + labels + FPS |
| `alerts.png`    | Alerts page: history table with a selected alert's snapshot preview |
| `settings.png`  | Settings page: notification channel status + "Send test notification" |

Tip: seed some data first — add a camera on the Cameras page, and (for alerts) run the
service against a source so alerts fire. A demo video can be linked from the README too.
